# Beads Configuration File
# All settings can also be set via environment variables (BD_* prefix)
# or overridden with command-line flags

# Issue prefix for this repository
# If not set, bd init will auto-detect from directory name
# issue-prefix: ""

# Enable JSON output by default
# json: false

# Default actor for audit trails (overridden by BD_ACTOR or --actor)
# actor: ""

# Git branch for beads commits (bd sync will commit to this branch)
# IMPORTANT: Set this for team projects so all clones use the same sync branch.
# This setting persists across clones (unlike database config which is gitignored).
# Can also use BEADS_SYNC_BRANCH env var for local override.

# Dolt server settings
# Port is intentionally NOT set here — bd derives a unique port per project
# from the absolute .beads/ path hash (range 13307-14306), preventing
# cross-project data leakage when running multiple projects simultaneously.
# To override: bd dolt set port <N>  (writes to gitignored metadata.json)
# dolt:
#   server_port: 3307

# Multi-repo configuration (experimental)
# Allows hydrating from multiple repositories and routing writes to the correct JSONL
# repos:
#   primary: "."
#   additional:
#     - ~/beads-planning

# Integration settings (access with 'bd config get/set')
# These are stored in the database, not in this file:
# - jira.url, jira.project
# - linear.url, linear.api-key
# - github.org, github.repo
