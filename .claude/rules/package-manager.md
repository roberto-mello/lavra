# Package Manager

Use `bun` for all package management, not npm or yarn.

- `bun install` instead of `npm install`
- `bun run` instead of `npm run`
- `bun add` instead of `npm install <pkg>`
- Lock files: `bun.lock` (delete `package-lock.json` if generated)
